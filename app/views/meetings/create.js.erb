alert('Período da DATA JÁ EXISTE !');

$('#form_meeting').html('<%= escape_javascript( render 'form' ) %>').css("display","none");
$('#form_meeting').fadeIn('slow', function() {});
$('#new_meeting').trigger("reset");
